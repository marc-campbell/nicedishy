apiVersion: v1
kind: Service
metadata:
  name: nicedishy-web
  labels:
    app: nicedishy-web
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 3000
    targetPort: http
  selector:
    app: nicedishy-web
